# ===============================
# = DATA SOURCE
# ===============================

# Server Port
server.port = 8081

spring.application.name= product-service
eureka.client.serviceUrl.defaultZone= ${EUREKA_URL:http://user:password@localhost:8761}/eureka/
spring.security.user.password= password

spring.profiles= heroku
eureka.instance.hostname= ${DOMAIN_NAME}
eureka.instance.nonSecurePort= 80
eureka.password= ${USER_PASSWORD:password}

# Set here configurations for the database connection

# Connection url for the database

spring.datasource.url = jdbc:mysql://sql732.main-hosting.eu:3306/u347172802_springwebstore?useUnicode=true&characterEncoding=utf8&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# Username and password
spring.datasource.username = u347172802_admin
spring.datasource.password = test12341234TEST

#spring.datasource.url = jdbc:mysql://docker-mysql:3306/springstore_schema?useUnicode=true&characterEncoding=utf8&useSSL=false&allowPublicKeyRetrieval=true
#spring.datasource.url = jdbc:mysql://sql11.freemysqlhosting.net:3306/sql11475947?useUnicode=true&characterEncoding=utf8&useSSL=false&allowPublicKeyRetrieval=true
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
#spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration

# Username and password
#spring.datasource.username = sql11475947
#spring.datasource.password = 7Ljnx582qV

# Keep the connection alive if idle for a long time (needed in production)
spring.datasource.testWhileIdle = true
spring.datasource.validationQuery = SELECT 1


# ===============================
# = JPA / HIBERNATE
# ===============================

# Hibernate - Show or not log for each sql query
spring.jpa.show-sql = false

# Hibernate ddl auto (create, create-drop, update): with "update" the database
# schema will be automatically updated accordingly to java entities found in
# the project
spring.jpa.hibernate.ddl-auto = update

# Allows Hibernate to generate SQL optimized for a particular DBMS
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect
spring.jpa.properties.hibernate.globally_quoted_identifiers=true

# ===============================
# = Eclipse plugin - ANSI Escape in Console
# ===============================
# To install the plugin in Eclipse select Help -> Install New Software... and click "Add.." to add the following URL:
# http://www.mihai-nita.net/eclipse
spring.main.banner-mode=off
spring.output.ansi.enabled=ALWAYS
